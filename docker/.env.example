# 必填（NCBI E-utilities 推荐配置，用于提高限速与合规标识）
PUBMED_API_KEY=
PUBMED_EMAIL=

# 可选：quick | deep
ABSTRACT_MODE=quick

# 可选：disabled | enabled | auto
FULLTEXT_MODE=disabled

# 可选：enabled | disabled
ENDNOTE_EXPORT=enabled

# Streamable HTTP 端口（默认 8745；docker-compose 会映射宿主机 8745 -> 容器 ${PORT}）
PORT=8745

