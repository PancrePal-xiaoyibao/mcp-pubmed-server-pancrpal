# PubMed API Configuration / PubMed API配置

# === 方式一：单个API密钥（向后兼容）===
PUBMED_API_KEY=your_pubmed_api_key_here
PUBMED_EMAIL=your_email@example.com

# === 方式二：多API密钥池 ===
# 创建 api-keys.json 文件（参见 api-keys.json.example）
# 如果 api-keys.json 存在，将优先于 PUBMED_API_KEY 环境变量
# 支持策略: round-robin（轮询）/ failover（主备）/ random（随机）

# Optional / 可选配置
# PUBMED_API_KEY可以从 https://www.ncbi.nlm.nih.gov/account/settings/ 获取
# 单key限速: 10次/秒，无key限速: 3次/秒

# 摘要截断模式：quick | deep
# quick：1500 字符（快速检索，可能不包含完整摘要）
# deep：6000 字符（深度检索；建议模型上下文窗口 ≥ 120k tokens，批量查询更稳妥）
ABSTRACT_MODE=quick
# 全文模式：disabled | enabled | auto
# disabled：禁用全文功能（默认）
# enabled：启用全文检测，手动下载
# auto：启用全文检测，自动下载可用的OA论文
FULLTEXT_MODE=disabled
# EndNote导出配置
ENDNOTE_EXPORT=enabled  # enabled, disabled

# 传输模式：stdio | streamableHttp
# MCP_TRANSPORT=stdio
# 服务端口（streamableHttp 模式下使用，默认 8745）
# PORT=8745
